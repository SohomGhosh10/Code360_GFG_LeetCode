class Solution:
    def countOfElements(self, arr, x):
        # Code Here
        n = len(arr)
        count = 0
        
        for i in range(n):
            if x >= arr[i]:
                count += 1
        return count
